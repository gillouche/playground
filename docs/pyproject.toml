[project]
name = "playground-docs"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "zensical",
    "pyyaml",
    "strawberry-graphql",
    "httpx>=0.28.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []

[tool.uv]
package = false
