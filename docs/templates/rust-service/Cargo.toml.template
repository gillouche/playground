[package]
name = "{{SERVICE_NAME}}"
version = "0.1.0"
edition = "2021"

[dependencies]
# Add dependencies here, then run 'cargo generate-lockfile'
# Example:
# axum = "0.7"
# tokio = { version = "1", features = ["full"] }
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"

[dev-dependencies]
# Test dependencies
