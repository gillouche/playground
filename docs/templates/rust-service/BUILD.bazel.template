# Template: Replace {{SERVICE_NAME}} with your service name
# Template: Replace {{APP_NAME}} with your app name (e.g., demo-app)

load("//tools:rust_defs.bzl", "rust_application")
load("@crates//:defs.bzl", "all_crate_deps")

rust_application(
    name = "{{SERVICE_NAME}}",
    srcs = glob(["src/**/*.rs"]),
    deps = all_crate_deps(),
    unit_tests = glob(["tests/unit/**/*.rs"]),
    integration_tests = glob(["tests/integration/**/*.rs"]),
    test_deps = all_crate_deps(test = True),
)
