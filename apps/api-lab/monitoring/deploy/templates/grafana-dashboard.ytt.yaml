#@ load("@ytt:data", "data")
#@ load("@ytt:json", "json")

#@ def dashboard():
#@   env = data.values.env
#@   app = data.values.app
#@   return {
#@     "annotations": {"list": []},
#@     "editable": True,
#@     "panels": [
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
#@         "id": 1,
#@         "targets": [{"expr": "sum(rate(http_requests_total{app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m]))", "legendFormat": "Total RPS", "refId": "A"}],
#@         "title": "Requests Per Second (Total)",
#@         "type": "timeseries"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
#@         "id": 2,
#@         "targets": [{"expr": "sum by (status) (rate(http_requests_total{app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m]))", "legendFormat": "{{status}}", "refId": "A"}],
#@         "title": "RPS by Status Code",
#@         "type": "timeseries"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
#@         "id": 3,
#@         "targets": [
#@           {"expr": "histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket{app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m])))", "legendFormat": "P99", "refId": "A"},
#@           {"expr": "histogram_quantile(0.95, sum by (le) (rate(http_request_duration_seconds_bucket{app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m])))", "legendFormat": "P95", "refId": "B"},
#@           {"expr": "histogram_quantile(0.50, sum by (le) (rate(http_request_duration_seconds_bucket{app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m])))", "legendFormat": "P50", "refId": "C"}
#@         ],
#@         "title": "Request Latency",
#@         "type": "timeseries"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
#@         "id": 4,
#@         "targets": [{"expr": "sum(rate(http_requests_total{status=~\"5..\", app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m])) / sum(rate(http_requests_total{app=\"" + app + "\", component=~\"${service}\", environment=\"" + env + "\"}[1m]))", "legendFormat": "Error Rate", "refId": "A"}],
#@         "title": "Error Rate",
#@         "type": "timeseries"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16},
#@         "id": 5,
#@         "targets": [
#@           {"expr": "sum(rate(api_lab_cache_hits_total{environment=\"" + env + "\"}[1m]))", "legendFormat": "Hits", "refId": "A"},
#@           {"expr": "sum(rate(api_lab_cache_misses_total{environment=\"" + env + "\"}[1m]))", "legendFormat": "Misses", "refId": "B"}
#@         ],
#@         "title": "Cache Hit/Miss Ratio",
#@         "type": "timeseries"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16},
#@         "id": 6,
#@         "targets": [{"expr": "histogram_quantile(0.95, sum by (le) (rate(api_lab_db_query_duration_seconds_bucket{environment=\"" + env + "\"}[1m])))", "legendFormat": "P95 DB Query", "refId": "A"}],
#@         "title": "DB Query Latency",
#@         "type": "timeseries"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 4, "w": 6, "x": 0, "y": 24},
#@         "id": 7,
#@         "targets": [{"expr": "api_lab_books_available{environment=\"" + env + "\"}", "legendFormat": "Available", "refId": "A"}],
#@         "title": "Available Books",
#@         "type": "stat"
#@       },
#@       {
#@         "datasource": {"type": "prometheus", "uid": "${datasource}"},
#@         "gridPos": {"h": 4, "w": 6, "x": 6, "y": 24},
#@         "id": 8,
#@         "targets": [{"expr": "api_lab_active_reservations{environment=\"" + env + "\"}", "legendFormat": "Active", "refId": "A"}],
#@         "title": "Active Reservations",
#@         "type": "stat"
#@       }
#@     ],
#@     "refresh": "5s",
#@     "schemaVersion": 38,
#@     "tags": ["api-lab", "prometheus"],
#@     "templating": {
#@       "list": [
#@         {"name": "datasource", "type": "datasource", "query": "prometheus", "hide": 2},
#@         {"name": "service", "type": "query", "query": {"query": "label_values(http_requests_total{app=\"" + app + "\", environment=\"" + env + "\"}, component)", "refId": "StandardVariableQuery"}, "refresh": 1, "sort": 1}
#@       ]
#@     },
#@     "time": {"from": "now-5m", "to": "now"},
#@     "title": app + " dashboard - " + env,
#@     "uid": app + "-dashboard-" + env
#@   }
#@ end

#@ if data.values.env != "sandbox":
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-lab-dashboard
  labels:
    grafana_dashboard: "1"
    component: #@ data.values.component
data: #@ { ("api-lab-" + data.values.env + ".json"): json.encode(dashboard()) }
#@ end
