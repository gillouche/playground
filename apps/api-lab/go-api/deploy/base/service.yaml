apiVersion: v1
kind: Service
metadata:
  name: go-api
spec:
  selector:
    component: go-api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 50051
      targetPort: 50051
      name: grpc
