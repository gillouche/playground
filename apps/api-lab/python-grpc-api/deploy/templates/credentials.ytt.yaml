#@ load("@ytt:data", "data")

#@ if data.values.env != "sandbox":
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: python-api-credentials
spec:
  encryptedData:
    POSTGRES_PASSWORD: PLACEHOLDER_SEALED_SECRET
    REDIS_PASSWORD: PLACEHOLDER_SEALED_SECRET
  template:
    metadata:
      name: python-api-credentials
    type: Opaque
#@ end
