#@ load("@ytt:data", "data")

#@ if data.values.env == "sandbox":
apiVersion: apps/v1
kind: Deployment
#@ else:
apiVersion: argoproj.io/v1alpha1
kind: Rollout
#@ end
metadata:
  name: python-grpc-api
  labels:
    component: python-grpc-api
    app: #@ data.values.app
    environment: #@ data.values.env
spec:
  replicas: 1
  #@ if data.values.env != "sandbox":
  revisionHistoryLimit: 3
  #@ end
  selector:
    matchLabels:
      component: python-grpc-api
  #@ if data.values.env != "sandbox":
  strategy:
    canary:
      analysis:
        templates:
          - templateName: success-rate
          - templateName: error-rate
        args:
          - name: service
            value: python-grpc-api
      steps:
      - setWeight: 20
      - pause: {duration: 30s}
      - setWeight: 100
  #@ end
  template:
    metadata:
      labels:
        component: python-grpc-api
        app: #@ data.values.app
        environment: #@ data.values.env
    spec:
      #@ if data.values.env != "sandbox":
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: topology.kubernetes.io/node-group
                operator: In
                values:
                - rpi
      #@ end
      containers:
      - name: python-grpc-api
        image: nexus.gillouche.homelab/docker-hosted/api-lab/python-grpc-api
        #@ if data.values.env == "sandbox":
        imagePullPolicy: Never
        #@ else:
        imagePullPolicy: IfNotPresent
        #@ end
        #@ if data.values.env != "sandbox":
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        #@ end
        envFrom:
        - configMapRef:
            name: python-grpc-api-config
        #@ if data.values.env != "sandbox":
        - secretRef:
            name: api-lab-credentials
        #@ else:
        - secretRef:
            name: python-grpc-api-secrets
        #@ end
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - containerPort: 50051
          name: grpc
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          grpc:
            port: 50051
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          grpc:
            port: 50051
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          grpc:
            port: 50051
          failureThreshold: 30
          periodSeconds: 10
      #@ if data.values.env != "sandbox":
      volumes:
      - name: tmp
        emptyDir: {}
      #@ end
