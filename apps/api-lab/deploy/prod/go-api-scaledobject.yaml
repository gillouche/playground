apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: go-api
  labels:
    component: go-api
    app: api-lab
    environment: prod
spec:
  scaleTargetRef:
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    name: go-api
  minReplicaCount: 1
  maxReplicaCount: 1
  pollingInterval: 30
  cooldownPeriod: 300
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "50"
