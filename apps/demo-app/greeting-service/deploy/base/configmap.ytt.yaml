#@ load("@ytt:data", "data")

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: #@ data.values.component + "-config"
  labels:
    app: #@ data.values.app
data:
  ENVIRONMENT: #@ data.values.env
  APP: #@ data.values.app
