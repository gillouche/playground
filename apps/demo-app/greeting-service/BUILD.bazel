load("//tools:defs.bzl", "application")

application(
    name = "greeting-service",
    language = "python",
    srcs = glob(["src/**/*.py"]),
    unit_tests = glob(["tests/unit/**/*.py"]),
    integration_tests = glob(["tests/integration/**/*.py"]),
)
