[project]
name = "demo-concept"
version = "0.1.0"
description = "Demo concept application"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
]


[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "ruff>=0.14.13",
    "httpx",
    "pytest-pythonpath>=0.7.3",
]

[tool.pytest.ini_options]
pythonpath = "src"

[tool.uv]
